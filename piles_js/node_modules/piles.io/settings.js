exports = exports || {}

var yaml = require('yaml')
,   fs = require('fs')
;

// Settings
exports.settings = yaml.eval(String(fs.readFileSync('/Users/cwilcox/Projects/piles.io/settings.yml')));

exports.setting = function(name) {
    return exports.settings[name]
}